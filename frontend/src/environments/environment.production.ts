// Production environment — API calls go directly to the Render.com backend.
// After deploying on Render, replace the URL below with your actual service URL.
export const environment = {
  production: true,
  apiBase: 'https://ytoget.onrender.com',
};
